[project]
name = "eth-protocol-fellowship-site"
version = "0"
description = "Build environment for the EPF dev-update site (zensical)."
requires-python = ">=3.13"
dependencies = [
    # Pre-1.0 doc tooling, upper-bounded to the tested minor (matches cuere).
    "zensical>=0.0.45,<0.1",
    # The Updates section will be generated by our own zensical-updates sidecar
    # (clean-room rewrite, separate repo). Added here once that ships; until then
    # the Updates page is a static stub.
]

[tool.uv]
package = false
